$("#comment_<%= @post.id %> .comment-show").html("<%= j(render 'comments/comment', post: @post, comment: @comment ) %>");
$("#comment_form").val("");
<!--フラッシュメッセージの非同期通信化-->
$('#flash-message').html("<%= j(render 'layouts/flash_messages') %>");